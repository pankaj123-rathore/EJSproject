<!DOCTYPE html>
<html lang="en">
<head>
  <title>Sign In | BazaarGhar</title>
  <%- include("__bootstrap.ejs") %>
  <style>
    body {
      background: linear-gradient(to right, #f5f7fa, #c3cfe2);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .form-control {
      border-radius: 12px;
    }

    .btn-custom {
      background-color: #ffcc00;
      color: black;
      font-weight: 600;
      border-radius: 12px;
    }

    .btn-custom:hover {
      background-color: #ffdb4d;
    }

    .form-title {
      font-size: 1.5rem;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <%- include("__header.ejs") %>

  <div class="container d-flex justify-content-center align-items-center" style="height: 90vh;">
    <div class="card p-4" style="width: 100%; max-width: 400px;">
      <div class="text-center mb-4">
        <h3 class="form-title">Sign In to LokhMart</h3>
        <p class="text-muted small">Welcome back! Please login to continue.</p>
      </div>

      <form action="/user/signin" method="post">
        <div class="mb-3">
          <label for="email" class="form-label">Email Address</label>
          <input id="email" name="email" type="email" class="form-control" placeholder="Enter your email" required>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input id="password" name="password" type="password" class="form-control" placeholder="Enter your password" required>
        </div>

        <button type="submit" class="btn btn-custom w-100">Sign In</button>
      </form>

      <div class="text-center mt-3">
        <small>Don't have an account? <a href="/sign-up" class="text-decoration-none text-primary">Sign up</a></small>
      </div>
    </div>
  </div>

</body>
</html>
